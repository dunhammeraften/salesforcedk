<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Time_in_status__c</fullName>
    <description>Time spent in status</description>
    <externalId>false</externalId>
    <formula>IF( ISBLANK(Change_Time__c),
TEXT( FLOOR( NOW() - Set_Time__c ) ) &amp; &quot; days &quot;
&amp; TEXT( FLOOR( MOD( (NOW() - Set_Time__c ) * 24, 24 ) ) ) &amp; &quot; hours &quot;
&amp; TEXT( ROUND( MOD( (NOW() - Set_Time__c ) * 24 * 60, 60 ), 0 ) ) &amp; &quot; minutes&quot;,
Age__c )</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>How long time has the case been in the status</inlineHelpText>
    <label>Time in status</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
